# 安全策略

Kairo CLI 会访问源代码、执行工具并连接外部模型或服务。我们重视能够突破工作区边界、
绕过审批、泄露凭据或导致未授权代码执行的安全问题。

## 支持范围

当前项目仍处于 `0.1.x` 开发阶段，仅最新提交和最新发布版本接受安全修复。旧版本不提供
长期安全维护；报告问题前请先确认最新版本是否仍可复现。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要为尚未修复的
漏洞创建公开 Issue、Discussion 或 Pull Request。

报告中请包含：

- 受影响的版本、提交和运行环境；
- 漏洞类型、影响范围和必要前置条件；
- 最小复现步骤或验证代码；
- 已知缓解措施（如有）；
- 报告者希望使用的署名方式。

请删除 API Key、访问令牌、个人数据和无关的私有代码。维护者会通过私密报告跟进确认、
修复和披露安排；在修复公开前，请避免传播可利用细节。

## 安全边界

以下行为不应被视为安全边界：

- `--dangerously-skip-approvals` 明确授权后的工具执行；
- 用户主动安装并执行未经审查的 Skill 或 MCP 服务；
- 用户将密钥、代码或记忆内容发送给自行配置的第三方模型或 embedding 服务；
- 在无操作系统隔离的环境中，把 Kairo CLI 当作恶意代码沙箱。

项目仍欢迎报告这些场景中违反既有文档承诺、绕过明确限制或默认配置不安全的问题。
